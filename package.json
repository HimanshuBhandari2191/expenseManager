{
  "name": "mini-expense-tracker-monorepo",
  "version": "1.0.0",
  "scripts": {
  "install-all": "npm install --prefix server && npm install --include=dev --prefix client",
  "rebuild-sqlite": "npm rebuild sqlite3 --prefix server",
  "build-frontend": "npm run build --prefix client",
  "move-frontend": "rm -rf server/public && mv client/dist server/public",
  "build": "npm run install-all && npm run rebuild-sqlite && npm run build-frontend && npm run move-frontend",
  "start": "node server/server.js"
}
}