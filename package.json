{
  "name": "mini-expense-tracker-monorepo",
  "version": "1.0.0",
  "scripts": {
    "install-all": "npm install --prefix client && npm install --prefix client",
    "build-frontend": "npm run build --prefix client",
    "move-frontend": "rm -rf client/public && mv client/dist client/public",
    "build": "npm run install-all && npm run build-client && npm run move-client",
    "start": "node server/server.js"
  }
}